@model IEnumerable<EXSM3935_C_Sharp_Final_Project.Models.Client>

@{
    ViewData["Title"] = "Clients";
}

<h1>Clients</h1>

<p>
    <a asp-action="Create">Create New Client</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                Client ID No.
            </th>
            <th>
                <!--@Html.DisplayNameFor(model => model.FirstName)-->
                First Name
            </th>
            <th>
                <!--@Html.DisplayNameFor(model => model.LastName)-->
                Last Name
            </th>
            <th>
                <!--@Html.DisplayNameFor(model => model.BirthDate)-->
                Birth Date
            </th>
            <th>
                <!--@Html.DisplayNameFor(model => model.HomeAddress)-->
                Home Address
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelIetm => item.FormattedClientId)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.FirstName)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.LastName)
                </td>
                <td>
                    <!--@Html.DisplayFor(modelItem => item.BirthDate)-->
                @Html.DisplayFor(modelItem => item.StrBday)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.HomeAddress)
                </td>
                <td>
                    <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                    <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                    <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>
